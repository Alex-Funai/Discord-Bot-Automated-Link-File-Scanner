package discord;


public interface Converter {

    /**
     * [B]
     * convertSha256() ::
     * @param URL  Pass in a URL to convert into a SHA256, SHA1, or MDA5 hex-string.
     * Convert an input URL to SHA256 + MDA for passing into different scanners. Doing this in anticipation of handling
     * the different argument variables for each scan, and simplifying the program by considering a common variable.
     */
/*    default String Converter(String convert) {
        String Converter = DigestUtils.sha256Hex(URL);
        String mdahex = DigestUtils.md5Hex(URL);
        return convert;
    }*/

    class String  {


    }
}

